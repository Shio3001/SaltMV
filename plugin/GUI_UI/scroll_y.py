class parts:
    def UI_set(self, data):

        data = data.operation["plugin"]["other"]["scroll"].CentralRole().main(data, 1)

        data.edit_size(x=20, y=400, space=2)
        #data.edit_percentage_percentage(position=0, size=50)

        return data
