class parts:
    def UI_set(self, UI_auxiliary):

        UI_auxiliary = UI_auxiliary.operation["plugin"]["other"]["scroll"].CentralRole().main(UI_auxiliary, 0)

        #UI_auxiliary.set_pxf_slope(sta_px=400, end_px=20, space=10)
        #UI_auxiliary.edit_percentage_percentage(position=0, size=50)

        return UI_auxiliary
