class parts:
    def UI_set(self, UI_data):

        UI_data = UI_data.operation["plugin"]["other"]["scroll"].CentralRole().main(UI_data, 1)

        #data.set_pxf_slope(sta_px=20, end_px=400, space=10)
        #data.edit_percentage_percentage(position=0, size=50)

        return UI_data
